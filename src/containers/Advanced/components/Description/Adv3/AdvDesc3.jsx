import React from "react";
import ChapterDesc from "../../../../../components/Contents/ChapterDesc";

const AdvDesc3 = () => {
  return (
    <ChapterDesc>
      Due to the deterministic behavior of the blockchain, it is widely known
      that generating randomness in the blockchain is quite a difficult task.
      There have been many efforts to produce a real random number. A typical
      method is to create a random number by submitting and collecting numbers
      from various subjects inside or outside the blockchain.
    </ChapterDesc>
  );
};

export default AdvDesc3;
